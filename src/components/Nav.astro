---
---

<nav class="fixed top-0 left-0 right-0 z-50 bg-white/95 backdrop-blur-sm border-b border-[#E0E0E0]">
  <div class="max-w-5xl mx-auto px-6 md:px-10 py-4 flex items-center justify-between">

    <!-- PC: Nav links 左寄せ -->
    <div class="hidden md:flex items-center gap-8">
      <a href="#"    class="text-[11px] font-light tracking-[0.2em] text-[#444] hover:text-black transition-colors duration-200">About me</a>
      <span class="text-[11px] font-light tracking-[0.2em] text-[#CCCCCC] cursor-default">Work</span>
      <span class="text-[11px] font-light tracking-[0.2em] text-[#CCCCCC] cursor-default">Hoby</span>
      <span class="text-[11px] font-light tracking-[0.2em] text-[#CCCCCC] cursor-default">News</span>
    </div>

    <!-- PC: Contact (disabled) -->
    <span class="hidden md:block text-[11px] font-light tracking-[0.2em] px-4 py-2 border border-[#CCCCCC] text-[#CCCCCC] cursor-default">
      Contact
    </span>

    <!-- Mobile: ハンバーガーボタン -->
    <button
      id="menu-btn"
      class="md:hidden flex flex-col justify-center gap-[5px] w-6 h-6 cursor-pointer"
      aria-label="メニュー"
    >
      <span class="block w-full h-px bg-black transition-all duration-200"></span>
      <span class="block w-full h-px bg-black transition-all duration-200"></span>
      <span class="block w-full h-px bg-black transition-all duration-200"></span>
    </button>

  </div>

  <!-- Mobile: ドロップダウンメニュー -->
  <div id="mobile-menu" class="hidden md:hidden border-t border-[#E0E0E0] bg-white/95 px-6 py-5 flex flex-col gap-5">
    <a href="#"    class="text-[12px] font-light tracking-[0.2em] text-[#444] hover:text-black transition-colors duration-200">About me</a>
    <span class="text-[12px] font-light tracking-[0.2em] text-[#CCCCCC] cursor-default">Work</span>
    <span class="text-[12px] font-light tracking-[0.2em] text-[#CCCCCC] cursor-default">Hoby</span>
    <span class="text-[12px] font-light tracking-[0.2em] text-[#CCCCCC] cursor-default">News</span>
    <span class="text-[12px] font-light tracking-[0.2em] text-[#CCCCCC] cursor-default">Contact</span>
  </div>
</nav>

<script>
  const btn = document.getElementById('menu-btn')
  const menu = document.getElementById('mobile-menu')

  btn?.addEventListener('click', () => {
    menu?.classList.toggle('hidden')
  })

  // リンクをタップしたらメニューを閉じる
  menu?.querySelectorAll('a').forEach(a => {
    a.addEventListener('click', () => menu.classList.add('hidden'))
  })
</script>
